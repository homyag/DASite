class Jumbotron {
  static selector() {
    return "[data-jumbotron]";
  }

  constructor(node) {
    this.node = node;
    this.init();
  }

  init() {
    // Инициализация компонента
  }
}

export default Jumbotron;
